# 员工管理系统 - 项目结构说明

## 项目概述
本项目是一个前后端分离的员工管理系统，包含三个角色：管理员、员工、部门。系统采用 Spring Boot 作为后端框架，Vue 3 作为前端框架，实现了员工和部门的 CRUD 功能以及登录认证。

## 目录结构

### 1. 后端目录结构 (Spring Boot)
```
src/main/java/com/example/workrecode/
├── common/              # 公共组件
│   ├── GlobalExceptionHandler.java    # 全局异常处理
│   └── Result.java                     # 统一响应结果封装
├── config/              # 配置类
│   └── CorsConfig.java                 # 跨域配置
├── controller/          # 控制器层（API接口）
│   ├── DepartmentController.java      # 部门管理接口
│   ├── EmployeeController.java        # 员工管理接口
│   └── UserController.java            # 用户登录接口
├── entity/              # 实体类（数据库映射）
│   ├── Department.java                 # 部门实体
│   ├── Employee.java                   # 员工实体
│   └── User.java                       # 用户实体
├── mapper/              # 数据访问层
│   ├── DepartmentMapper.java           # 部门数据访问
│   ├── EmployeeMapper.java             # 员工数据访问
│   └── UserMapper.java                 # 用户数据访问
├── service/             # 业务逻辑层
│   ├── impl/                           # 业务实现
│   │   ├── DepartmentServiceImpl.java  # 部门业务实现
│   │   ├── EmployeeServiceImpl.java    # 员工业务实现
│   │   └── UserServiceImpl.java        # 用户业务实现
│   ├── DepartmentService.java          # 部门业务接口
│   ├── EmployeeService.java            # 员工业务接口
│   └── UserService.java                # 用户业务接口
└── WorkrecodeApplication.java          # 应用启动类


src/main/resources/
├── db/migration/        # 数据库迁移脚本
│   └── V1__init_schema.sql             # 初始化数据库表结构
├── mapper/              # MyBatis XML映射文件
│   ├── EmployeeMapper.xml              # 员工映射
│   └── UserMapper.xml                  # 用户映射
├── application.yml                      # 应用配置文件
└── init.sql                             # 数据库初始化数据
```

### 2. 前端目录结构 (Vue 3)
```
frontend/
├── src/
│   ├── router/              # 路由配置
│   │   └── index.js         # 路由定义
│   ├── views/               # 页面组件
│   │   ├── DepartmentView.vue    # 部门管理页面
│   │   ├── EmployeeView.vue      # 员工管理页面
│   │   ├── HomeView.vue          # 首页
│   │   └── LoginView.vue         # 登录页面
│   ├── App.vue              # 根组件
│   ├── axios.js             # Axios配置
│   └── main.js              # 应用入口
├── index.html               # HTML模板
├── package.json             # 项目依赖配置
└── vite.config.js           # Vite构建配置
```

## 核心功能模块

### 1. 用户登录模块
- **后端**：UserController.java 提供登录接口，UserService.java 实现验证逻辑
- **前端**：LoginView.vue 登录页面，使用Element Plus表单组件
- **数据存储**：user表存储用户名、密码和角色信息

### 2. 员工管理模块
- **后端**：EmployeeController.java 提供CRUD接口，EmployeeService.java 实现业务逻辑
- **前端**：EmployeeView.vue 员工管理页面，包含列表、添加、编辑、删除功能
- **数据存储**：employee表存储员工信息，与department表关联

### 3. 部门管理模块
- **后端**：DepartmentController.java 提供CRUD接口，DepartmentService.java 实现业务逻辑
- **前端**：DepartmentView.vue 部门管理页面，包含列表、添加、编辑、删除功能
- **数据存储**：department表存储部门信息

## 技术栈

### 后端技术栈
- **框架**：Spring Boot 2.7.15
- **ORM框架**：MyBatis Plus 3.5.3.1
- **数据库**：MySQL 8.0.33
- **API风格**：RESTful
- **跨域处理**：CORS配置

### 前端技术栈
- **框架**：Vue 3
- **构建工具**：Vite
- **UI组件库**：Element Plus
- **路由管理**：Vue Router 4
- **HTTP请求**：Axios

## 数据库设计

### 1. 用户表 (user)
```sql
CREATE TABLE IF NOT EXISTS user (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    role VARCHAR(255) NOT NULL,
    name VARCHAR(255) NOT NULL
);
```

### 2. 部门表 (department)
```sql
CREATE TABLE IF NOT EXISTS department (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL
);
```

### 3. 员工表 (employee)
```sql
CREATE TABLE IF NOT EXISTS employee (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    gender VARCHAR(10) NOT NULL,
    department_id BIGINT NOT NULL,
    FOREIGN KEY (department_id) REFERENCES department(id)
);
```

## 启动说明

### 后端启动
1. 确保 MySQL 数据库已启动，创建名为 `workrecode` 的数据库
2. 修改 `src/main/resources/application.yml` 中的数据库配置（用户名和密码）
3. 运行 `WorkrecodeApplication.java` 启动后端服务

### 前端启动
1. 进入 `frontend` 目录
2. 执行 `npm install` 安装依赖
3. 执行 `npm run dev` 启动前端开发服务器
4. 访问 `http://localhost:5173`

## 默认账号
- **管理员账号**：admin / admin123

## 主要功能说明

1. **登录功能**：用户输入用户名和密码进行登录，系统验证身份后跳转到首页
2. **员工管理**：
   - 查看员工列表
   - 添加新员工
   - 编辑员工信息
   - 删除员工
   - 根据姓名搜索员工
3. **部门管理**：
   - 查看部门列表
   - 添加新部门
   - 编辑部门信息
   - 删除部门
4. **权限控制**：管理员可以查看和管理所有员工和部门信息

## 注意事项
1. 确保前后端服务都已启动
2. 数据库配置正确，且已创建对应的数据库
3. 前端使用 Element Plus 组件库，确保依赖已正确安装
4. 系统默认使用 MySQL 数据库，如需切换其他数据库，需修改相关配置

## 扩展建议
1. 添加分页功能，处理大量数据
2. 实现更细粒度的权限控制
3. 添加数据导入导出功能
4. 实现数据统计和图表展示
5. 添加员工考勤和绩效评估功能